name: "olist_project"
version: "1.0.0"
config-version: 2

profile: "olist_project"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_project:
    staging:
      +materialized: view
      +schema: olist_staging
    marts:
      +materialized: table
      +schema: olist_analytics
